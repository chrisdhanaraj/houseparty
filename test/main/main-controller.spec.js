describe('Main Ctrl:', function() {
  beforeEach(module('houseparty.common'));
  beforeEach(module('main.team'));

  var $controller;

  beforeEach(inject(function(_$controller_) {
    $controller = _$controller_;
  }));

  describe('$scope.remainder', function() {
    it('should return true when no remainder', function() {

    });

    it('should return false when there is a remainder', function() {

    });

  });



});